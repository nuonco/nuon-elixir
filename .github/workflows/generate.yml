---
name: Generate SDK
on:
  workflow_dispatch:
env:
  BRANCH_NAME: "nuonbot/regenerate"
  COMMIT_MESSAGE: "chore: regenerate from new api version"
jobs:
  generate-sdk:
    name: Generate SDK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Enable commit signing
        uses: crazy-max/ghaction-import-gpg@v6
        with:
          gpg_private_key: ${{ secrets.NUON_BOT_GITHUB_TOKEN }}
          passphrase: ""
          git_user_signingkey: true
          git_commit_gpgsign: true
      - name: Generate SDK
        run: |
          ./scripts/generate.sh
      - name: Commit changes
        run: |
          git checkout -b $BRANCH_NAME
          git add .
          git commit -m "$COMMIT_MESSAGE"
          git push -u origin $BRANCH_NAME
      - name: Open PR
        run: |
          gh pr create --title "$COMMIT_MESSAGE" --body "$COMMIT_MESSAGE"
          gh pr merge --auto --delete-branch --squash "$BRANCH_NAME"
